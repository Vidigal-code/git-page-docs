# Configuration

All runtime options are configured in `gitpagedocs/config.json`.

## Site settings

- `defaultLanguage`
- `HideThemeSelector`
- `ThemeDefault`
- `ActiveNavigation`
- `IconImageMenuHeader`
