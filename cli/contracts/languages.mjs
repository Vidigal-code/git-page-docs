/**
 * Languages the generator ships UI strings and docs for, in menu order. This
 * is the list written to `gitpagedocs/langs.json`; every entry must have a
 * `langs/<lang>.json` bundle and a docs content module.
 */
export const SUPPORTED_LANGUAGES = Object.freeze(["en", "pt", "es"]);
